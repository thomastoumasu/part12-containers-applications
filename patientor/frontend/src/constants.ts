// export const apiBaseUrl = 'http://localhost:3001/api';  // if backend and frontend are run separately
export const apiBaseUrl = 'http://localhost:8080/api'; // if run within container with reverse proxy
